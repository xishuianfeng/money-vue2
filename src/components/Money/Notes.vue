<template>
    <div>
        <label class="note">
            <span class="name">{{this.fieldName}}</span>
            <input type="text"
                   v-model = 'value'
                   :placeholder="this.placeholder">
        </label>
    </div>

</template>
<script lang="ts">
  import Vue from 'vue'
  import {Component, Prop, Watch} from 'vue-property-decorator';

  @Component
  export default class Notes extends Vue{
    value = '';

    @Prop({required:true}) fieldName!:string; //required:true 必填
    @Prop() placeholder?:string; 

    @Watch('value')
    onValueChanged(value:string){
      this.$emit('update:value',value);
    };
  }
</script>

<style lang="scss">
    .note{
        font-size: 14px;
        background: #f5f5f5;
        display: block;
        padding-left: 0 16px;
        display: flex;
        align-items: center;
        .name{
        padding-left: 16px;
      }
    input{
        height: 64px;
        flex-grow: 1;
        background: transparent;
        border: none;
        padding: 16px;
      }
    }
</style>